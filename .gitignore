.DS_Store
*.log
node_modules/
# Private strategy doc — kept local / in ~/.claude/plans, never published
ECONOMICS.md
# Wrangler local state (simulated D1/KV) — never committed
.wrangler/
worker/.dev.vars
