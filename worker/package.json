{
  "name": "brightspark-worker",
  "private": true,
  "type": "module",
  "description": "Brightspark 11+ monetisation backend — magic-link auth, entitlement and Stripe. Separate dev-only sub-project; the static site stays zero-build and does not depend on this at build time.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy:staging": "wrangler deploy --env staging",
    "deploy:prod": "wrangler deploy --env production",
    "migrate:local": "wrangler d1 migrations apply brightspark --local",
    "migrate:staging": "wrangler d1 migrations apply brightspark --env staging --remote",
    "migrate:prod": "wrangler d1 migrations apply brightspark --env production --remote",
    "test": "node --test test/*.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
